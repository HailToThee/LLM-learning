一、原始 Skip-Gram 的痛点
  
  原始 Skip-Gram:给定中心词 $c$,预测窗口内的上下文词 $o$。输出层是对整个词表做 
  softmax:

  $$P(o \mid c) = \frac{\exp(\mathbf{u}_o^\top \mathbf{v}c)}{\sum{w=1}^{W}
  \exp(\mathbf{u}_w^\top \mathbf{v}_c)}$$

  问题全在分母那个求和 $\sum_{w=1}^{W}$ 上:

  1. 计算量爆炸:词表 $W$ 通常几十万到几百万。每来一个训练样本,梯度都要对所有 $W$
  个词更新一遍,复杂度 $O(W)$。
  2.
  大部分更新是浪费:绝大多数词跟当前中心词根本没共现关系,算它们的分数纯属陪跑。

  训练一次 Wikipedia 量级的语料,这种做法慢到不现实。

  ---
  二、为什么要负采样 —— 把"多分类"降维成"二分类"
  
  负采样(Negative Sampling)的核心思路是换任务:

  ▎ 不再问"给定中心词,词表里哪个词是上下文?"(多分类)
  ▎
  ▎ 改成问"这个(中心词,目标词)配对,是真的上下文对,还是瞎凑的?"(二分类)

  目标函数变成:

  $$\log \sigma(\mathbf{u}o^\top \mathbf{v}c) + \sum{k=1}^{K} \mathbb{E}{w_k
  \sim P_n(w)}\big[\log \sigma(-\mathbf{u}_{w_k}^\top \mathbf{v}_c)\big]$$

  - 第一项:正样本(真实上下文对),希望内积大,sigmoid 趋近 1。
  - 第二项:$K$ 个负样本(随机采的词),希望内积小,sigmoid 趋近 0。

  复杂度从 $O(W)$ 直接降到 $O(K+1)$,其中 $K$ 通常只要 5~20。这是数量级的提速。

  采样分布一般用变形的 unigram 分布:$P_n(w) \propto
  \text{count}(w)^{0.75}$。指数 0.75 的作用是稍微提高低频词被采中的概率,否则高频
  词(the、of)会垄断负样本,模型学不到有用信号。

  ---
  三、为什么输入变成"pair(配对)"而不是中心词?
  
  这正是负采样"换任务"的直接结果,两种模型的输入结构根本不同:

  ┌──────────────────┬─────────────────────────┬─────────────────────────────┐
  │                  │     原始 Skip-Gram      │      负采样 Skip-Gram       │
  ├──────────────────┼─────────────────────────┼─────────────────────────────┤
  │ 输入             │ 中心词 $c$              │ 配对 $(c, t)$               │
  ├──────────────────┼─────────────────────────┼─────────────────────────────┤
  │ 输出             │ 词表上的概率分布($W$    │ 该配对为真的概率(1          │
  │                  │ 维)                     │ 维,sigmoid)                 │
  ├──────────────────┼─────────────────────────┼─────────────────────────────┤
  │ 任务             │ 多分类                  │ 二分类                      │
  ├──────────────────┼─────────────────────────┼─────────────────────────────┤
  │ 每步更新的词向量 │ 全部 $W$ 个             │ 只有 $K+1$ 个               │
  └──────────────────┴─────────────────────────┴─────────────────────────────┘

  为什么必须是 pair?
  因为二分类的判断单位本来就是"一对词的关系",不是"一个词"。模型要回答的是"$(c,
  t)$ 这对能不能共现",输入自然是这个配对本身。

  - 真实窗口里的 $(c, o)$ → 正样本对,label = 1
  - 中心词不变,目标词换成随机采的 $(c, w_k)$ → 负样本对,label = 0

  所以训练集从"中心词 → 上下文"重构成了"一堆 (中心词, 目标词, 标签)
  三元组",这就是你看到输入是 pair 的原因。

  ---
  四、顺带一提
  
  负采样不是唯一解,另一个常见方案是层次化 softmax(Hierarchical 
  Softmax)——用霍夫曼树把 $O(W)$ 压成 $O(\log W)$。两者目的相同(避开全词表
  softmax),只是手段不同。word2vec 原论文里这两种都给了。

  一句话总结:负采样是为了避开全词表 softmax 的 $O(W)$ 计算开销,代价是把任务从多
  分类改成二分类;而二分类的输入单位天然是"配对",所以输入就从中心词变成了
  (中心词, 目标词) pair。

  需要的话,我可以进一步讲:指数 0.75 
  的数学直觉、为什么负采样在数学上可以看作对真实 softmax 分布的近似(MLE 
  角度),或者层级 softmax 和负采样的对比取舍。